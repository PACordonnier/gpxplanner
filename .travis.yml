dist: 
  - xenial
language: node_js
before_install:
  - cd server
before_script:
  - docker-compose -f ./server/docker-compose.yml up -d
  - curl -X PUT -u admin:password localhost:5984/gpxplanner
node_js:
  - 16
services:
# - couchdb
  - docker
# addons:
#   apt:
#     sources:
#       - ppa:couchdb/stable
#     packages:
#     - couchdb